import Button from "../../components/Button";
import MainLayout from "../../components/MainLayout";
import "./index.css";

const ConnectWallet = (props) => {
    return (
        <MainLayout>
            <Button text="CONNECT WITH METAMASK" method={() => {}}/>
        </MainLayout>
    );
}

export default ConnectWallet;