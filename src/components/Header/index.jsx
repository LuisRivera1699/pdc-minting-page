import "./index.css"
import logo from "../../assets/logo.webp";

const Header = (props) => {
    return (
        <header>
            <img src={logo} alt=""/>
        </header>
    );
}

export default Header;